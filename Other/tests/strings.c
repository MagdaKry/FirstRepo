#include <stdlib.h>
#include <stdio.h>
#include <string.h>

int main ()
{

    char message[] = "Welcome to C Programming";
    char message2[24];
    printf ("%d", strlen(message2));
    printf (message2);
    strcpy(message2,message);
    printf (message2);
    printf ("%d", strlen(message2));
    
}
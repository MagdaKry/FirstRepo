#include <stdio.h>

int main(void){

    int length = 2;
    int width = 4;
    int perimeter = 0;

    perimeter = 2 * (length + width);

    printf ("Perimeter of the rectangle is %d", perimeter);
    
}